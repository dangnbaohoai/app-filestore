import TagCreate from "./TagCreate";
import TagEdit from "./TagEdit";
import {TagList} from "./TagList";

const tags = {
    list: TagList,
    create: TagCreate,
    edit: TagEdit,
}
export default tags